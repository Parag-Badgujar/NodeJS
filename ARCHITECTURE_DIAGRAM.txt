╔══════════════════════════════════════════════════════════════════════════════╗
║                   COMPLETE DOCKER & CI/CD ARCHITECTURE                        ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│                            LOCAL DEVELOPMENT SETUP                           │
└──────────────────────────────────────────────────────────────────────────────┘

                         Your Windows/Mac/Linux Machine
                         ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                                    ↓
                          ┌─────────────────┐
                          │  Docker Desktop │
                          │   (Running)     │
                          └────────┬────────┘
                                   ↓
                    ┌──────────────────────────────┐
                    │    Docker Compose Network    │
                    │   (nodejs_network - bridge)  │
                    │                              │
                    │  ┌───────────────────────┐  │
                    │  │   Node.js Container   │  │
                    │  │  ┌─────────────────┐  │  │
                    │  │  │ Port: 4001      │  │  │
                    │  │  │ Env: NODE_ENV   │  │  │
                    │  │  │ Run: npm run dev│  │  │
                    │  │  │ Health: enabled │  │  │
                    │  │  └─────────────────┘  │  │
                    │  └───────────────────────┘  │
                    │            ↓                 │
                    │  ┌───────────────────────┐  │
                    │  │ PostgreSQL Container  │  │
                    │  │ ┌─────────────────┐   │  │
                    │  │ │ Port: 5432      │   │  │
                    │  │ │ Image: postgres │   │  │
                    │  │ │ 15-alpine       │   │  │
                    │  │ │ Volume:         │   │  │
                    │  │ │ postgres_data   │   │  │
                    │  │ │ Health: enabled │   │  │
                    │  │ └─────────────────┘   │  │
                    │  └───────────────────────┘  │
                    │                              │
                    └──────────────┬───────────────┘
                                   ↓
                    localhost:4001  (Your App)
                    localhost:5432  (Database)


┌──────────────────────────────────────────────────────────────────────────────┐
│                           FILE STRUCTURE (PROJECT ROOT)                      │
└──────────────────────────────────────────────────────────────────────────────┘

NodeJS/
│
├─ 🐳 DOCKER FILES
│  ├── Dockerfile                    ← Multi-stage build
│  │   ├── Stage 1: Builder
│  │   │   ├── FROM node:20-alpine
│  │   │   ├── npm ci
│  │   │   └── npm run build
│  │   └── Stage 2: Runtime
│  │       ├── FROM node:20-alpine
│  │       ├── npm ci --production
│  │       └── COPY dist from builder
│  │
│  ├── docker-compose.yml            ← Local dev setup
│  │   ├── services:
│  │   │   ├── db (PostgreSQL)
│  │   │   └── app (Node.js)
│  │   ├── volumes: postgres_data
│  │   └── networks: nodejs_network
│  │
│  ├── .dockerignore                 ← Build optimization
│  │   ├── node_modules
│  │   ├── .git
│  │   └── coverage
│  │
│  ├── docker-setup.bat              ← Windows automation
│  ├── docker-setup.sh               ← Linux/Mac automation
│  │
│  └── .env                          ← Runtime config (created)
│      ├── PORT=4001
│      ├── DB_HOST=db
│      └── DB_PASSWORD=root
│
├─ 🔄 CI/CD FILES
│  └── .github/
│      └── workflows/
│          └── ci-cd.yml             ← GitHub Actions
│              ├── Triggers: push, PR
│              ├── Job 1: Build & Test
│              ├── Job 2: Build & Push Docker
│              └── Job 3: Deploy notification
│
├─ 📚 DOCUMENTATION
│  ├── README.md                     ← Project overview
│  ├── SETUP_COMPLETE.md             ← Completion summary
│  ├── DOCKER_SETUP_SUMMARY.txt      ← Setup guide
│  ├── DOCKER_CICD_GUIDE.md          ← Comprehensive guide
│  ├── DOCKER_CHEATSHEET.md          ← Commands reference
│  └── QUICK_REFERENCE.txt           ← Quick start
│
├─ 📝 CONFIGURATION
│  ├── .env.example                  ← Template
│  ├── .env                          ← Actual (created)
│  ├── .gitignore                    ← Git ignore
│  ├── package.json                  ← npm config
│  ├── tsconfig.json                 ← TypeScript config
│  └── jest.config.js                ← Test config
│
├─ 💻 APPLICATION CODE
│  ├── src/
│  │   ├── app.ts
│  │   ├── server.ts
│  │   ├── configs/
│  │   ├── controllers/
│  │   ├── routes/
│  │   └── models/
│  │
│  └── utils/
│      └── encryption.ts
│
└─ 🗂️ PROJECT STRUCTURE
   ├── .git/                        ← Git history
   ├── .github/                     ← GitHub config
   ├── node_modules/                ← Dependencies
   └── dist/                        ← Built files (generated)


┌──────────────────────────────────────────────────────────────────────────────┐
│                            CI/CD PIPELINE FLOW                               │
└──────────────────────────────────────────────────────────────────────────────┘

                    ┌─────────────────────┐
                    │   Your Code Changes │
                    └──────────┬──────────┘
                               │
                        ┌──────▼──────┐
                        │ git push ⬆  │
                        │ to GitHub   │
                        └──────┬──────┘
                               │
                      ┌────────▼────────┐
                      │  GitHub Actions │  ← Webhook triggered
                      │   CI/CD Engine  │
                      └────────┬────────┘
                               │
                ┌──────────────┴──────────────┐
                │                             │
         ┌──────▼────────┐           ┌───────▼───────┐
         │  Job 1:       │           │  Job 2:       │
         │  Build&Test   │           │  Build&Push   │
         │               │           │  Docker Image │
         ├───────────────┤           ├───────────────┤
         │ ✓ Checkout    │           │ ✓ Setup       │
         │ ✓ Node.js     │           │ ✓ BuildKit    │
         │ ✓ Install     │           │ ✓ Login to    │
         │ ✓ Lint        │           │   ghcr.io     │
         │ ✓ Build TS    │           │ ✓ Build image │
         │ ✓ Run tests   │           │ ✓ Push image  │
         │ ✓ Pass? →────┼──────────→│ ✓ Tag image   │
         └──────────────┘           └───────┬───────┘
                                            │
                                    ┌───────▼────────┐
                                    │  GitHub        │
                                    │  Container     │
                                    │  Registry      │
                                    │  (ghcr.io)     │
                                    │                │
                                    │ Image stored   │
                                    │ Ready deploy   │
                                    └────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                         DATA FLOW IN CONTAINERS                              │
└──────────────────────────────────────────────────────────────────────────────┘

                    Outside Docker              Inside Docker
                    ══════════════              ═════════════
                    
Your Application
  ↓
localhost:4001  ←→  Port Mapping  ←→  nodejs_app:4001 (Container IP)
  ↓                                        ↓
Your Browser                         Express.js App
  ↓                                        ↓
http://localhost:4001          Process request/response
  ↓                                        ↓
                                     Connect to Database
                                        ↓
localhost:5432  ←→  Port Mapping  ←→  postgres:5432 (db service)
  ↓                                        ↓
Your DB Client                       PostgreSQL Process
                                        ↓
                                   Return query results
                                        ↓
                                   Node.js receives
                                        ↓
                                   Send response
                                        ↓
Your Browser receives HTML/JSON


┌──────────────────────────────────────────────────────────────────────────────┐
│                         NETWORK COMMUNICATION                                │
└──────────────────────────────────────────────────────────────────────────────┘

                        Docker Compose Network
                         (nodejs_network)
                        ═══════════════════════
                                
                    ┌─────────────────────┐
                    │   Your Machine      │
                    │  localhost:4001     │
                    │  localhost:5432     │
                    └──────────┬──────────┘
                               │ Port Mapping
                    ┌──────────▼──────────┐
                    │  Docker Network     │
                    │  (Bridge Network)   │
                    │                     │
                    │ ┌────────────────┐  │
                    │ │ nodejs_app     │  │
                    │ │ container      │  │
                    │ │ IP: x.x.x.x    │  │
                    │ │ Port: 4001     │  │
                    │ └────────┬───────┘  │
                    │          │          │
                    │        DNS: db      │  ← Service name
                    │          │          │
                    │ ┌────────▼───────┐  │
                    │ │ postgres       │  │
                    │ │ container      │  │
                    │ │ IP: x.x.x.y    │  │
                    │ │ Port: 5432     │  │
                    │ └────────────────┘  │
                    │                     │
                    └─────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                         ENVIRONMENT VARIABLES                                │
└──────────────────────────────────────────────────────────────────────────────┘

.env File (Local Machine)
┌────────────────────────────────┐
│ PORT=4001                      │
│ HOST=0.0.0.0                   │
│ NODE_ENV=development           │
│ DB_HOST=db                     │ ← Service name (NOT localhost)
│ DB_PORT=5432                   │
│ DB_USER=postgres               │
│ DB_PASSWORD=root               │
│ DB_DATABASE=mobilestore        │
│ DIALECT=postgres               │
└────────────────────────────────┘
         ↓ Read by ↓
    docker-compose.yml
         ↓ Passes to ↓
    ┌─────────────────┐       ┌──────────────────┐
    │ app container   │       │ db container     │
    │ Sets ENV vars   │       │ Sets ENV vars    │
    │ PORT=4001       │       │ POSTGRES_USER    │
    │ DB_HOST=db      │       │ POSTGRES_PASSWORD│
    │ DB_PORT=5432    │       │ POSTGRES_DB      │
    └─────────────────┘       └──────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                          DEPLOYMENT LIFECYCLE                                │
└──────────────────────────────────────────────────────────────────────────────┘

1. DEVELOPMENT
   ┌──────────────────────┐
   │ Local Machine        │
   │ Docker Compose       │
   │ npm run dev (watch)  │
   │ Hot reload enabled   │
   └──────────────────────┘

2. TESTING
   ┌──────────────────────┐
   │ GitHub Actions       │
   │ npm test             │
   │ npm run lint         │
   │ npm run build        │
   └──────────────────────┘

3. BUILD
   ┌──────────────────────┐
   │ Docker Build         │
   │ Multi-stage build    │
   │ Image optimization   │
   └──────────────────────┘

4. PUSH
   ┌──────────────────────┐
   │ GitHub Container     │
   │ Registry             │
   │ ghcr.io              │
   └──────────────────────┘

5. DEPLOYMENT (Ready)
   ┌──────────────────────┐
   │ Cloud Platform       │
   │ AWS ECS              │
   │ Google Cloud Run     │
   │ Kubernetes           │
   └──────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                           QUICK START COMMANDS                               │
└──────────────────────────────────────────────────────────────────────────────┘

Windows (Easiest):
  ► Double-click: docker-setup.bat

Linux/Mac:
  $ chmod +x docker-setup.sh
  $ ./docker-setup.sh

Manual (Any OS):
  $ cp .env.example .env
  $ docker-compose up -d
  $ docker-compose logs -f app

Access:
  Browser:  http://localhost:4001
  Database: localhost:5432


═════════════════════════════════════════════════════════════════════════════════
                         🎉 SETUP IS COMPLETE! 🎉
═════════════════════════════════════════════════════════════════════════════════
